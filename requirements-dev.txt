-r requirements.txt
# pytest, pytest-asyncio and pytest-cov are pinned exactly by
# pytest-homeassistant-custom-component (currently pytest==9.0.3), so declaring
# our own floors here fights those pins rather than adding anything. A
# dependabot bump to pytest>=9.1.1 had already made this file unsatisfiable;
# nobody noticed because CI hardcoded its install list and never read it.
pytest-homeassistant-custom-component>=0.13.355
