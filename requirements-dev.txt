-r requirements.txt
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
pytest-homeassistant-custom-component>=0.13.355
# homeassistant.components.tts imports mutagen at module level. Home Assistant
# installs it at runtime from the tts manifest, but the test harness does not,
# so it has to be declared here for the suite to import the integration.
mutagen>=1.47.0
